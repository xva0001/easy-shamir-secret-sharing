{
  "name": "secretsjs_grempe_rewrite",
  "version": "1.0.4",
  "main": "src/secrets_re.ts",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "tsc test/test.ts && node test/test.js"
  },
  "dependencies": {
    "http": "^0.0.1-security",
    "node-forge": "^1.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.10",
    "@types/node-forge": "^1.3.11"
  },
  "description": "`secrets_re.ts` 是一個用於秘密分享和重建的 TypeScript 庫，基於 Shamir 的秘密分享方案。此庫允許將一個秘密分割成多個分享，並且需要一定數量的分享才能重建原始秘密。\r `secrets_re.ts` is a TypeScript library for secret sharing and reconstruction based on Sh",
  "directories": {
    "test": "test"
  },
  "author": "xva001",
  "license": "MIT"
}
